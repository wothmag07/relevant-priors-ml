# Development + evaluation dependencies (not installed into the Docker image).
# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
httpx>=0.27   # required by fastapi.testclient (starlette dep), not pulled in transitively

# Plotting for eval/plot_metrics.py
matplotlib>=3.8

# Lint + type-check
ruff>=0.5
mypy>=1.10
